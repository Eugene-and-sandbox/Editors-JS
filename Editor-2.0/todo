1. After clicking on the "Create list" or "Create table" buttons, the modal window doesn`t hide after table crating. Function added. Checked. Part of code that was commented, working correctly
2. After login/logout imitation, and after it again you opening the login window, the login window has broken styles and will close when clicking on this one
3. Password input field validating even when the input field is empty
4. Doesn`t work strikethrough button. Styles added, checked
5. User Authentication Form doesn`t checking if login and/or password are not correct. If there are login and/or password are with mistakes, User Authentication Form will closed
